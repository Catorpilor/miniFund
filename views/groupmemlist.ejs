<% if(grouplist.length) { %>
    <select class="multiselect" multiple="multiple" name="groupmemnames">
        <% grouplist.forEach(function(puser){ %>
        	<option value="<%= puser.name %>"><%= puser.name %></option>
        <% }) %>
    </select>
<% } %> 
